package usecases_test

import (
	. "github.com/onsi/ginkgo"
	. "github.com/onsi/gomega"
)

var _ = Describe("Create User", func() {
	Context("When the username and password is given", func() {
		It("passes", func() {
			Expect(true).To(BeTrue())
		})
	})
})
